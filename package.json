{
  "name": "web-home",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "package": "vite build && node scripts/package.mjs",
    "set-defaults": "node scripts/set-defaults.mjs"
  },
  "devDependencies": {
    "vite": "^8.0.16",
    "archiver": "^7.0.1"
  },
  "dependencies": {
    "gridstack": "^12.6.0"
  }
}
